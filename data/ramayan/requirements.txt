# Main Packages
# if running the commands without a venv
# then these main packages should be installed
# via system package manager like apt, dnf or pacman instead of pip
typer
rich
pydantic
requests
pyyaml

# Other
shubhlipi

# command to install dependencies
# pip install -r requirements.txt