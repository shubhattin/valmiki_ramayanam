{
	"name": "Sanskrit Transliteration",
	"initializeCommand": "echo 'Skipping default npm install'",
	"postCreateCommand": "npm i -g pnpm && pnpm install && cd data/ramayan && pip install -r requirements.txt && cd ../.."
}
