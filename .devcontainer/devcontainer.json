{
  "name": "Sanskrit Transliteration",
  "initializeCommand": "echo 'Skipping default npm install'",
  "postCreateCommand": "git config --global pull.rebase false && npm i -g pnpm tsx && pnpm install && cd data/ramayan && pip install -r requirements.txt && cd ../.."
}
